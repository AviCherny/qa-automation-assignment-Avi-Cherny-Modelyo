[pytest]
addopts = -v --tb=short -p no:playwright
testpaths = tests
pythonpath = .
markers =
    ui: UI tests (Swag Labs)
    api: API tests (JSONPlaceholder)
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %H:%M:%S
